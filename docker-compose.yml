# Family Book — Full Stack
# Bring-up order: conduit → bridges → familybook
# See SPEC.md § Matrix as Universal Bridge Layer

services:
  familybook:
    build: .
    ports: ["${PORT:-8000}:8000"]
    volumes: ["./data:/data"]
    env_file: .env
    environment:
      - MATRIX_HOMESERVER=http://conduit:6167
    depends_on:
      conduit:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    restart: unless-stopped

  conduit:
    image: ${CONDUIT_IMAGE:?Set CONDUIT_IMAGE to a reviewed immutable image reference}
    volumes: ["./conduit-data:/var/lib/matrix-conduit"]
    environment:
      CONDUIT_SERVER_NAME: ${CONDUIT_SERVER_NAME:-family.example.com}
      CONDUIT_DATABASE_BACKEND: rocksdb
      CONDUIT_PORT: 6167
      CONDUIT_ALLOW_REGISTRATION: "false"
      CONDUIT_MAX_REQUEST_SIZE: 20000000  # 20MB for media
    ports: ["6167:6167"]
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:6167/_matrix/client/versions"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped

  mautrix-whatsapp:
    image: ${MAUTRIX_WHATSAPP_IMAGE:?Set MAUTRIX_WHATSAPP_IMAGE to a reviewed immutable image reference}
    volumes: ["./mautrix-whatsapp:/data"]
    depends_on:
      conduit:
        condition: service_healthy
    restart: unless-stopped

  mautrix-telegram:
    image: ${MAUTRIX_TELEGRAM_IMAGE:?Set MAUTRIX_TELEGRAM_IMAGE to a reviewed immutable image reference}
    volumes: ["./mautrix-telegram:/data"]
    depends_on:
      conduit:
        condition: service_healthy
    restart: unless-stopped

  mautrix-signal:
    image: ${MAUTRIX_SIGNAL_IMAGE:?Set MAUTRIX_SIGNAL_IMAGE to a reviewed immutable image reference}
    volumes: ["./mautrix-signal:/data"]
    depends_on:
      conduit:
        condition: service_healthy
    restart: unless-stopped

  mautrix-meta:
    image: ${MAUTRIX_META_IMAGE:?Set MAUTRIX_META_IMAGE to a reviewed immutable image reference}
    volumes: ["./mautrix-meta:/data"]
    depends_on:
      conduit:
        condition: service_healthy
    restart: unless-stopped
